import fetch from 'node-fetch';

export const getFile = async (id: string) => {

	// await axios.request({
	// 	method: 'post',
	// 	url: 'http://'+window.location.hostname+":5656/info/"+id,
	// }).then(async (res: any) => {
	// 	console.log(res);
	// })
}