export enum AuthenticationAction {
	LOGIN,
	LOGOUT,
};

export enum UploadOptionAction {
	TOGGLE_PROTECTED,
	TOGGLE_HIDDEN,
	SET,
}

export enum UploadHistoryAction {
	ADD,
	DELETE,
	PROGRESS,
	CLEAR
}